#include<bits/stdc++.h>
#define ll long long
#define rep(n) for(int i=0;i<n;i++)
using namespace std;
int main()
{
	ll n;
	cin>>n;
	int max_curr=0;
	ll ans=0;
	rep(n){
		int no;
		cin>>no;
		max_curr=max(no,max_curr);
		ans+=max_curr-no;
	}
	
	cout<<ans;
}
